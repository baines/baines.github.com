<span class="date">{{ page.date | date: "%d-%b-%Y" }}</span>
<div>
<a href="{{ page.url }}">{{ page.title }}</a>
{{ CONTENT }}
<iframe width="100%" height="166" scrolling="no" frameborder="no" src="{{ page.embedsrc }}"></iframe>
</div>
